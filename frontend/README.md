# FYP Frontend — React + Vite (JSX) + Tailwind 3.3.3 + Monaco

Gamified coding platform frontend. Built with **React (JSX)**, **Vite**, **Tailwind CSS 3.3.3**, and **Monaco Editor**.  
Talks to a Node/Express backend via `VITE_API_URL`.

---

## ⚙️ Tech Stack
- **React (JSX)** + **Vite**
- **Tailwind CSS 3.3.3** (PostCSS 8, Autoprefixer 10)
- **Monaco Editor** via `@monaco-editor/react`
- Optional: **React Router** (can be added later)

---

## 🚀 Getting Started

```bash
# from repo root
cd frontend

# install deps
npm i

# copy env and edit if needed
cp .env.example .env

# start dev server
npm run dev
# open the printed URL (usually http://localhost:5173)
